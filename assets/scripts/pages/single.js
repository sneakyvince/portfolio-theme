// import gsap from 'gsap'
// import scrollama from 'scrollama';
// // let elements = document.query
//
// const observer = new IntersectionObserver( ( entries ) => {
//
//
// 	entries.forEach( entry => {
//
// 		// If the element is in view, do animation
// 		if ( entry.intersectionRatio > 0 ) {
// 			gsap.from( entry.target, {
// 				opacity    : 0,
// 				y          : 25,
// 				duration   : 0.5,
// 				clearProps : 'all',
// 			} );
// 			observer.unobserve( entry.target );
// 		}
// 	} )
// } );
//
//
// let article = document.getElementById('entry-content');
// let elements = article.children.classList.add(' foo');
//
// console.log(elements);
//
// if ( elements ) {
// 	// Add an intersection observer to each element
// 	elements.forEach( element => {
// 		observer.observe( element )
// 	} );
// }
//
// observe( test, function(x) {
// 	gsap.from( entry.target, {
// 		opacity    : 0,
// 		y          : 25,
// 		duration   : 0.5,
// 		clearProps : 'all',
// 	} );
// } );
//
// function observe( tes, callback ) {
//
// }



